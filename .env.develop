# --- VERTICAL 360 - ENVIRONMENT CONFIGURATION (DEV) ---

# Conexión Base a MariaDB (Requiere permisos de CREATE/DROP DATABASE)
DB_SERVER=localhost
DB_USER=root
DB_PASSWORD=root

# Nombres de Bases de Datos
DB_SHARED_NAME=vertical360_shared
DB_TENANT_PREFIX=v360_tenant_

# JWT Security
JWT_KEY=CambiaEstaClavePorUnaMuyLargaYSegura_!2025
JWT_ISSUER=Vertical360.API
JWT_AUDIENCE=Vertical360.Web

# Email SMTP (Notificaciones de administración)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=johan.arangor@gmail.com
SMTP_PASS=vmcblrhnslixwrsr

# Origin CORS
CORS_ORIGIN=http://localhost:5173